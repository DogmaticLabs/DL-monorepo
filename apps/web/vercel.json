{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/web && pnpm install --frozen-lockfile && pnpm build",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/web && pnpm install --frozen-lockfile",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
